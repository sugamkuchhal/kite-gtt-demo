name: Unified Script Runner v2

on:

  #schedule:
    #- cron: "0,30 4-9 * * 1-5"
    #- cron: "0 11 * * 1-5"
    #- cron: "0,30 3 * * *"

  workflow_dispatch:
    inputs:
      script:
        description: "Script mode: script to run (e.g. zerodha_tick_size)."
        required: false
        default: ""
      mode:
        description: "Run mode: eod | midday | refresh"
        required: false
        type: choice
        options:
          - SELECT
          - eod
          - midday
          - refresh
          - normalize
          - teleport_full

# Overlap safety: queue new runs until the current one finishes
concurrency:
  group: unified-script-runner
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v5

      - name: Decide auto action window
        id: plan
        env:
          MODE: ${{ github.event.inputs.mode }}
          SCRIPT: ${{ github.event.inputs.script }}
          CRON_EXPR: ${{ github.event.schedule }}
        run: |
          set -euo pipefail

          MODE_VAL="${MODE:-SELECT}"
          CMD_VAL="${SCRIPT:-}"
          CRON_EXPR_VAL="${CRON_EXPR:-}"
          EVENT_NAME="${{ github.event_name }}"

          TODAY=$(TZ=Asia/Kolkata date +%d-%m-%Y)
          HOLIDAY_FILE="holidays.txt"
          IS_HOLIDAY=false

          # Check holidays for ANY trigger type, not just schedule
          if [ -f "$HOLIDAY_FILE" ] && grep -Fxq "$TODAY" "$HOLIDAY_FILE"; then
            IS_HOLIDAY=true
            echo "::notice::Holiday detected ($TODAY)"
          fi
          
          ACTION="none"
          
          # 1. Script override
          if [ -n "$CMD_VAL" ]; then
            ACTION="script"
          
          # 2. Manual mode (explicit)
          elif [ "$EVENT_NAME" = "workflow_dispatch" ] && [ "$MODE_VAL" != "SELECT" ]; then
            if [ "$MODE_VAL" = "midday" ] && [ "$IS_HOLIDAY" = "true" ]; then
              ACTION="none"
              echo "::notice::Holiday ($TODAY) → skipping midday run"
            elif [ "$MODE_VAL" = "eod" ] && [ "$IS_HOLIDAY" = "true" ]; then
              ACTION="eod_holiday"
            else
              ACTION="$MODE_VAL"
            fi
          
          # 3. Scheduled run → map known cron expressions to action
          elif [ "$EVENT_NAME" = "schedule" ]; then
            case "$CRON_EXPR_VAL" in
              "0,30 4-9 * * 1-5")
                if [ "$IS_HOLIDAY" = "true" ]; then
                  ACTION="none"
                  echo "::notice::Holiday ($TODAY) → skipping midday scheduled run"
                else
                  ACTION="midday"
                fi
                ;;
              "0 11 * * 1-5")
                if [ "$IS_HOLIDAY" = "true" ]; then
                  ACTION="eod_holiday"
                else
                  ACTION="eod"
                fi
                ;;
              "0,30 3 * * *")
                ACTION="refresh"
                ;;
              *)
                ACTION="none"
                ;;
            esac
          fi
          
          SHOULD_RUN=true
          if [ "$ACTION" = "none" ]; then
            SHOULD_RUN=false
            echo "::notice::No action selected (SELECT). Exiting."
          else
            echo "::notice::action=${ACTION} (event=${EVENT_NAME}, cron=${CRON_EXPR_VAL:-n/a}, ist=$(TZ=Asia/Kolkata date +%F\ %T))"
          fi

          echo "action=$ACTION" >> "$GITHUB_OUTPUT"
          echo "should_run=$SHOULD_RUN" >> "$GITHUB_OUTPUT"
          echo "command=$CMD_VAL" >> "$GITHUB_OUTPUT"
          echo "is_holiday=$IS_HOLIDAY" >> "$GITHUB_OUTPUT"

      - name: Set up Python
        uses: actions/setup-python@v6
        if: ${{ steps.plan.outputs.should_run == 'true' }}
        with:
          python-version: "3.12"
          cache: "pip"
          cache-dependency-path: requirements.txt

      - name: Write auth files (api_key/access_token)
        if: ${{ steps.plan.outputs.should_run == 'true' }}
        env:
          API_KEY: ${{ secrets.API_KEY }}
          ACCESS_TOKEN: ${{ secrets.ACCESS_TOKEN }}
        run: |
          set -euo pipefail
          echo "$API_KEY" > api_key.txt
          echo "$ACCESS_TOKEN" > access_token.txt

      - name: Check token validity
        id: token_check
        if: ${{ steps.plan.outputs.should_run == 'true' }}
        env:
          ACTION: ${{ steps.plan.outputs.action }}
        run: |
          set -euo pipefail
          if [ "$ACTION" = "refresh" ]; then
            echo "need_login=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          python3 -m pip install --quiet --target .deps_check kiteconnect
          if PYTHONPATH=".deps_check${PYTHONPATH:+:$PYTHONPATH}" python3 ensure_token.py --check-only; then
            echo "need_login=false" >> "$GITHUB_OUTPUT"
          else
            echo "need_login=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Install Google Chrome
        if: ${{ steps.plan.outputs.should_run == 'true' && steps.token_check.outputs.need_login == 'true' }}
        run: |
          sudo apt-get update -y
          sudo apt-get install -y wget unzip
          wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          sudo dpkg -i google-chrome-stable_current_amd64.deb || sudo apt-get -fy install
          google-chrome --version

      - name: Install dependencies
        if: ${{ steps.plan.outputs.should_run == 'true' }}
        run: |
          set -euo pipefail
          python3 -m venv .venv
          .venv/bin/pip install -r requirements.txt

      - name: Recreate non-auth secret files
        if: ${{ steps.plan.outputs.should_run == 'true' && steps.plan.outputs.action != 'refresh' }}
        env:
          CREDS_JSON: ${{ secrets.CREDS_JSON }}
          SMTP_TOKEN_JSON: ${{ secrets.SMTP_TOKEN_JSON }}
          TELEGRAM_TOKEN_JSON: ${{ secrets.TELEGRAM_TOKEN_JSON }}
        run: |
          set -euo pipefail
          [ -n "$CREDS_JSON" ] && printf '%s' "$CREDS_JSON" > creds.json
          [ -n "$SMTP_TOKEN_JSON" ] && printf '%s' "$SMTP_TOKEN_JSON" > smtp_token.json
          [ -n "$TELEGRAM_TOKEN_JSON" ] && printf '%s' "$TELEGRAM_TOKEN_JSON" > telegram_token.json
          ls -l creds.json smtp_token.json api_key.txt access_token.txt || true

      - name: Refresh ref_sheets.json
        if: ${{ steps.plan.outputs.should_run == 'true' && steps.plan.outputs.action != 'refresh' }}
        run: |
          set -euo pipefail
          if [ -f .venv/bin/activate ]; then source .venv/bin/activate; fi
          export PATH="$PWD/bin:$PATH"
          python3 refresh_ref_sheets_json.py

      - name: Run unified modes (script/refresh/midday/eod/auto)
        if: ${{ steps.plan.outputs.should_run == 'true' }}
        env:
          ACTION: ${{ steps.plan.outputs.action }}
          CMD_VAL: ${{ steps.plan.outputs.command }}
          GH_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          sudo -E bash -lc '
            set -euo pipefail
            if [ -f .venv/bin/activate ]; then
              source .venv/bin/activate
            else
              echo "::notice::No venv present; using system python."
            fi
            export PATH="$PWD/bin:$PATH"

            ensure_token_cmd() {
              ensure_token "$@"
            }

            # Script mode: run a single command (script wrapper)
            if [ "$ACTION" = "script" ]; then
              echo "::notice::mode=script → ensuring token, then running: ${CMD_VAL}"
              ensure_token_cmd
              eval "$CMD_VAL"
              exit 0
            fi

            # Refresh mode: force token refresh and exit
            if [ "$ACTION" = "refresh" ]; then
              echo "::notice::mode=refresh → forcing token refresh"
              ensure_token_cmd --force
              exit 0
            fi

            # Ensure token before running chains
            ensure_token_cmd

            if [ "$ACTION" = "midday" ]; then
              echo "::group::RUN: Mid-day chain"
              python3 zerodha_tick_size.py
              python3 set_field_false.py
              python3 fetch_all_gtts.py
              python3 fetch_all_orders.py
              bash combined_home_run_midday.sh
              echo "::endgroup::"
            elif [ "$ACTION" = "eod_holiday" ]; then
              echo "::group::RUN: EOD holiday chain"
              python3 zerodha_tick_size.py
              bash combined_fetcher.sh
              python3 zerodha_tick_size.py
              bash combined_feeder.sh
              python3 set_field_false.py
              python3 fetch_all_gtts.py
              python3 fifo_portfolio.py
              python3 fetch_holdings.py
              bash combined_main_run.sh
              bash combined_home_run.sh
              echo "::endgroup::"
            elif [ "$ACTION" = "eod" ]; then
              echo "::group::RUN: EOD chain"
              python3 zerodha_tick_size.py
              bash combined_fetcher.sh
              python3 zerodha_tick_size.py
              bash combined_feeder.sh
              python3 set_field_false.py
              python3 fetch_all_gtts.py
              bash combined_order_fetch_append.sh
              python3 fifo_portfolio.py
              python3 fetch_holdings.py
              bash combined_main_run.sh
              bash combined_home_run.sh
              echo "::endgroup::"
            elif [ "$ACTION" = "normalize" ]; then
              echo "::group::RUN: Normalize chain"
              bash combined_normalize_run.sh
              echo "::endgroup::"
            elif [ "$ACTION" = "teleport_full" ]; then
              echo "::group::RUN: Teleport Full"
              python3 data_teleporter.py --mode full
              echo "::endgroup::"
            else
              echo "::notice::action=$ACTION has no chain; exiting."
            fi
          '

      - name: Commit ref_sheets.json if changed
        if: ${{ steps.plan.outputs.should_run == 'true' && steps.plan.outputs.action != 'refresh' }}
        run: |
          set -euo pipefail
          sudo chown "$(whoami)" ref_sheets.json 2>/dev/null || true
          if git diff --quiet -- ref_sheets.json; then
            echo "::notice::No ref_sheets.json changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add ref_sheets.json
          git commit -m "chore: refresh ref_sheets.json [skip ci]"
          git pull --rebase origin main
          git push
          
